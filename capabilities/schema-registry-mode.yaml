naftiko: 1.0.0-alpha2
info:
  label: Confluent Schema Registry API — Mode
  description: 'Confluent Schema Registry API — Mode. 2 operations. Lead operation: Get global mode. Self-contained Naftiko
    capability covering one Confluent Schema Registry business surface.'
  tags:
  - Confluent Schema Registry
  - Mode
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    CONFLUENT_SCHEMA_REGISTRY_API_KEY: CONFLUENT_SCHEMA_REGISTRY_API_KEY
capability:
  consumes:
  - type: http
    namespace: schema-registry-mode
    baseUri: http://localhost:8081
    description: Confluent Schema Registry API — Mode business capability. Self-contained, no shared references.
    resources:
    - name: mode
      path: /mode
      operations:
      - name: gettoplevelmode
        method: GET
        description: Get global mode
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updatetoplevelmode
        method: PUT
        description: Update global mode
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: schema-registry-mode-rest
    port: 8080
    description: REST adapter for Confluent Schema Registry API — Mode. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/mode
      name: mode
      description: REST surface for mode.
      operations:
      - method: GET
        name: gettoplevelmode
        description: Get global mode
        call: schema-registry-mode.gettoplevelmode
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatetoplevelmode
        description: Update global mode
        call: schema-registry-mode.updatetoplevelmode
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: schema-registry-mode-mcp
    port: 9090
    transport: http
    description: MCP adapter for Confluent Schema Registry API — Mode. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: get-global-mode
      description: Get global mode
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: schema-registry-mode.gettoplevelmode
      outputParameters:
      - type: object
        mapping: $.
    - name: update-global-mode
      description: Update global mode
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: schema-registry-mode.updatetoplevelmode
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
